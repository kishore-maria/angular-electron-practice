import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-electron-practice',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
